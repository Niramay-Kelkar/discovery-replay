# Core schema / agent dependencies.
# The demo target app has its own list in target_app/requirements.txt.
pydantic>=2.11

# Discovery agent
anthropic>=1.4
playwright>=1.49        # needs: python -m playwright install chromium
python-dotenv>=1.0

# Replay escalation operator console
flask>=3.0

# Dev
pytest>=8
