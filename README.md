# discovery-replay
Computer-use automation: an LLM discovers how to complete a task once inside a UI-only legacy app, compiles it into a typed, replayable capability artifact, and replays it deterministically with no model in the loop.
