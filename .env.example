# Copy to .env and fill in. .env is git-ignored; this template is committed.

# Anthropic API key used by the discovery agent (agent.discover).
# Replay does not call an LLM and does not need this.
ANTHROPIC_API_KEY=

# Optional overrides
# TARGET_APP_BASE_URL=http://127.0.0.1:5001
