"""Hand-authored PolicySpec + InputBinding sets, one module per capability.

The compiler (``agent/compile.py``) merges one of these into the
mechanical transcript it reads off a trajectory. Nothing in here is or
could be derived from a discovery run -- it is authored deliberately,
with knowledge of the target app's failure modes.
"""
